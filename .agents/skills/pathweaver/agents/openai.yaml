interface:
  display_name: "Pathweaver"
  short_description: "Repo-specific patterns and workflows for pathweaver"
  default_prompt: "Use the pathweaver repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true